# 🚀 START HERE - Quick Start Guide

## Проект готов к использованию! ✅

---

## 📋 Проверка перед запуском

### Шаг 1: Валидация системы (30 секунд)

```bash
python validate_system.py
```

**Ожидается**:
```
🎉 ALL TESTS PASSED - SYSTEM IS READY!
```

Если тесты не прошли - смотрите раздел "Troubleshooting" ниже.

---

## 🎯 Три способа запуска

### Вариант 1: Быстрый тест (30-60 минут)

```bash
python main.py --quick-test --device cuda
```

**Результат**: 
- 10 моделей
- Демонстрация всех возможностей
- Проверка работоспособности

### Вариант 2: Средний запуск (4-6 часов)

```bash
python main.py --population-size 500 --device cuda
```

**Результат**:
- 500 начальных архитектур
- Полный 3-stage pipeline
- Top-10 Pareto-optimal моделей

### Вариант 3: Полный запуск (12-24 часа)

```bash
python main.py --population-size 3000 --device cuda --output-dir ./my_results
```

**Результат**:
- 3000 начальных архитектур
- Максимальное разнообразие
- Лучшие шансы найти SOTA

---

## 📁 Где найти результаты

После запуска проверьте:

```
./evo_runs/
├── knowledge_base.db       # База всех архитектур
├── pareto_stage2.png       # Визуализация Stage 2
├── pareto_final.png        # Финальный Pareto front
├── top_model_1.json        # Лучшая модель
├── top_model_2.json        # 2-я модель
├── ...
└── summary.json            # Итоговая статистика
```

---

## 🔍 Мониторинг процесса

### В консоли

```
==================================================
STAGE 1: Proxy Evaluation
==================================================
Evaluating candidates: 100%|██████████| 3000/3000
Selected 450 candidates from Stage 1

==================================================
STAGE 2: Refinement Training
==================================================
Training candidates: 100%|██████████| 450/450
Selected 100 candidates from Stage 2 (Pareto frontier)
...
```

### С WandB (опционально)

```bash
# Авторизация (один раз)
wandb login

# Запуск с логированием
python main.py --device cuda
```

Откройте ссылку из консоли для real-time мониторинга.

---

## 🛠️ Troubleshooting

### Проблема: CUDA Out of Memory

**Решение**:
```bash
python main.py --population-size 500 --device cuda
```

Или уменьшите в `config.yaml`:
```yaml
runtime:
  compute_resources:
    num_parallel_trials: 1  # Вместо 2
```

### Проблема: WandB authentication failed

**Решение**: Система продолжит работу без WandB
```
Warning: Failed to initialize WandB: ...
Continuing without WandB logging...
✓ Это нормально, система работает
```

### Проблема: Import errors

**Решение**:
```bash
pip install -r requirements.txt --upgrade
python test_imports.py
```

### Проблема: Слишком долго

**Решение**: Используйте `--quick-test`
```bash
python main.py --quick-test
```

---

## 📚 Документация

| Файл | Описание |
|------|----------|
| `README.md` | Полная документация проекта |
| `USAGE_GUIDE.md` | Практические примеры использования |
| `AUDIT_REPORT.md` | Технический отчет аудита |
| `POST_AUDIT_SUMMARY.md` | Итоги аудита |
| `config.yaml` | Пример конфигурации |

---

## 💡 Советы

### Для быстрых экспериментов

```bash
# CPU (медленно, но работает)
python main.py --quick-test --device cpu

# GPU (быстро)
python main.py --quick-test --device cuda
```

### Для максимальной новизны

Отредактируйте `config.yaml`:
```yaml
stage1_config:
  selection_criteria:
    novelty_score_weight: 0.5  # ↑ Больше новизны
```

### Для максимальной точности

```yaml
stage1_config:
  selection_criteria:
    novelty_score_weight: 0.1  # ↓ Меньше новизны
```

---

## 🎓 Что дальше?

После успешного запуска:

1. **Анализ результатов**:
   ```bash
   # Посмотрите лучшую модель
   cat ./evo_runs/top_model_1.json
   
   # Статистика
   cat ./evo_runs/summary.json
   ```

2. **Экспорт для использования**:
   ```python
   from src.search_space.search_space import ArchitectureGenome
   from src.search_space.architecture_generator import ArchitectureBuilder
   
   genome = ArchitectureGenome.from_dict(json.load(...))
   model = ArchitectureBuilder.build(genome)
   ```

3. **Эксперименты**:
   - Измените параметры в `config.yaml`
   - Попробуйте другие датасеты
   - Добавьте свои mutation operators

---

## ✅ Checklist

Перед полным запуском убедитесь:

- [x] `python validate_system.py` прошел успешно
- [x] Достаточно места на диске (~50GB рекомендуется)
- [x] GPU доступен (или готовы ждать на CPU)
- [x] Изучили `config.yaml` и `USAGE_GUIDE.md`

---

## 🆘 Нужна помощь?

1. **Проверьте документацию**: `README.md`, `USAGE_GUIDE.md`
2. **Запустите валидацию**: `python validate_system.py`
3. **Прочитайте AUDIT_REPORT.md**: Известные ограничения
4. **GitHub Issues**: Откройте issue с описанием проблемы

---

## 🎉 Готовы начать!

```bash
# Рекомендуемый первый запуск
python validate_system.py
python main.py --quick-test --device cuda
```

**Удачи в поиске novel архитектур!** 🧬🚀
